frappe.listview_settings['Purchase'] = {
  refresh: function(frm){
  }
  //  onload: function (listview) {
    //    if (frappe.user.has_role("System Manager")) {
      //      listview.page.add_menu_item(__("Update Country Names"), function () {
        //        // listview.call_for_selected_items(method, { 'status': 'Level 1 Approved' });
          //          frappe.call({
            //            method: "rights_management.rights_management.doctype.purchase.purchase.update_country_set_title",
              //          args: {
                //        },
                  //      freeze: true,
                    //    freeze_message: __("Updating"),
                      //  callback: function (r) {
                        //   console.log("updated")
                          // frappe.model.set_value(child.doctype, child.name, "countries", r.message.title);
                       // }
                   // })
           // })
       // }
   // }
};
